/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['rfvazcvvtrgdknkonewo.supabase.co'], // Tambahkan domain Supabase di sini
    },
  };
  
  export default nextConfig;
  